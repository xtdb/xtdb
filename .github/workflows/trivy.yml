name: Trivy Security Scan

on:
  workflow_dispatch:

jobs:
  trivy-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
    steps:
      - name: Check out the repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v5

      - name: Run Trivy against sample XTDBk8s
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.34.1
        with:
          scan-type: "fs"
          scan-ref: ./modules/azure/docs/azure-setup-guide/kubernetes/xtdb.yaml
          scanners: "vuln,secret,misconfig"
          format: 'template'
          output: 'trivy-report.html'
          template: '@.github/trivy/trivy-html-template.tpl'

      - name: HTML Trivy Report
        shell: bash
        if: always()
        run: 'cat $TRIVY_OUTPUT >> $GITHUB_STEP_SUMMARY'
