(require 'imdb.main)
(ns imdb.main)
(def s (future
           (run-system
            crux-options
            (fn [crux-system]
              (def crux crux-system)
              (Thread/sleep Long/MAX_VALUE)))))
crux
(injest-data crux)
(require 'imdb.main)
(ns imdb.main)
(def s (future
           (run-system
            crux-options
            (fn [crux-system]
              (def crux crux-system)
              (Thread/sleep Long/MAX_VALUE)))))
(injest-data crux)
(def s (future
           (run-system
            crux-options
            (fn [crux-system]
              (def crux crux-system)
              (Thread/sleep Long/MAX_VALUE)))))
